export type burnOptions = {
  file: string | true;
  model: string | true;
  description: string | true;
};

export type loadOptions = {
  file: string | true;
  model: string | true;
  weightHash: string | true;
};